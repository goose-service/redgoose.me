// path
// The server bundle is stored in `dist/`, while the client build is stored in
// `dist/client/`. Static files and generated head metadata live in the latter.
export const PATH_DIST = './dist/client'

// number
export const NUM_CACHE_MAX_AGE = 60 * 60 * 24 * 365 // 1 year
